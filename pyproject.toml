[project]
name = "bluebook-helper"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "lxml>=6.1.1",
    "lxml-stubs>=0.5.1",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
