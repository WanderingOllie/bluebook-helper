[project]
name = "bluebook-helper"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "lxml>=6.1.1",
    "lxml-stubs>=0.5.1",
    "openai>=3.0.0",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.2",
]

[tool.pytest.ini_options]
pythonpath = [".", "tests"]
